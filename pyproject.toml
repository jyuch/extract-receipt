[project]
name = "extract-receipt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.0.3",
]

[dependency-groups]
dev = [
    "mlflow>=3.5.0",
    "ruff>=0.14.1",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
